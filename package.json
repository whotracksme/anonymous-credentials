{
  "name": "anonymous-credentials",
  "version": "1.0.1",
  "description": "Anonymous credentials library",
  "main": "./lib/index.js",
  "scripts": {
    "test": "mocha --timeout 10000 --full-trace tests/tests.js",
    "bench": "node tests/bench.js",
    "native-install": "bash build-native.sh && CC=clang CXX=clang++ AR=llvm-ar node-gyp rebuild --verbose"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:whotracksme/anonymous-credentials.git"
  },
  "author": "WhoTracksMe",
  "license": "ISC",
  "dependencies": {
    "bindings": "1.5.0"
  },
  "devDependencies": {
    "chai": "4.3.4",
    "mocha": "9.1.3"
  },
  "engines": {
    "node": ">=6.0.0"
  },
  "gypfile": false
}
